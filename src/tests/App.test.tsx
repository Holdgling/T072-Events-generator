import React from "react";
import { render, fireEvent, waitForElement } from "@testing-library/react";

import App, { AppProps } from "../components/App";

describe("<App />", () => {
  test("should be tested when there will be things to test", async () => {
  });
});